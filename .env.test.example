# Integration tests (tests/integration/**) hit a real Supabase project.
# Copy to .env.test, or just rely on .env.local (loaded as a fallback).
# Use a NON-PRODUCTION project — the tests read RLS-protected tables as the
# QA seed users.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
